import {BaseElement} from './baseElement';

export interface Divider extends BaseElement {
    id: string;
    color: string;
    style: string;
    height: string;
    marginBot: string;
    marginTop: string;
    space: string;
}
